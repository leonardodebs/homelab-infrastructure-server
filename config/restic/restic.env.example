# Copiado pelo script install-backup-stack.sh para:
# /etc/homelab-backup/restic.env

RESTIC_REPOSITORY=/srv/backup/restic
RESTIC_PASSWORD_FILE=/etc/homelab-backup/restic-password
RESTIC_CACHE_DIR=/var/cache/restic-homelab
HOMELAB_PROJECT_DIR=/opt/homelab-infrastructure-server
BACKUP_MOUNT=/srv/backup
